#!/usr/bin/bash
set -euo pipefail

base="$DEV_CONTAINER_IMAGE"
target=${1?Pass the container as the first argument}
shift 1
devpod up --git-clone-recursive-submodules --fallback-image "$base" "$target" "$@"
