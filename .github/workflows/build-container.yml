name: Build Container

on:
  schedule:
    - cron: "25 14 1 * *"
  push:
    branches: [ "master" ]
    tags: [ "v*.*.*" ]
  pull_request:
    branches: [ "master" ]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build:
    permissions:
      contents: read
      packages: write
      id-token: write

    uses: FasterSpeeding/Piped/.github/workflows/build-container.yml@a7f9b8409db65c8e52b23cb89add60717293218c
    secrets: inherit
    with:
      architectures: '[{\"os\":\"ubuntu-24.04\",\"architecture\":\"amd64\"}]'
      image-name: '${{ github.repository }}-dx'
      maximise-space: true
